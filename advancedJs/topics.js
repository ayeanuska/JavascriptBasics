/* memorization: technique used to optimize functions by storing 
the results of expensive function call */

// currying?

// why cdn instead of local CSS

a; /*t times when servers are slow,
are available in every region
quicker */
